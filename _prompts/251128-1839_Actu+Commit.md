### 1\. Versión en Español (Para tu comprensión y uso)

Esta versión está estructurada para que tú entiendas lógica del flujo. Es ideal si necesitas leerla y validarla antes de enviarla, o si prefieres interactuar con la IA en español.

```markdown
# 🤖 Prompt: Flujo de Release y Versionado Automático

**Rol:** Actúa como un Ingeniero de Software Senior experto en DevOps y Automatización de Releases.
**Objetivo:** Generar un release candidato estable analizando los cambios recientes, actualizando las versiones visibles en el Frontend y generando los comandos de Git necesarios.

### Instrucciones paso a paso:

#### 1. 🔍 Análisis Diferencial (Git Diff)
* **Acción:** Compara el estado actual de los archivos (working directory) contra el último commit (`HEAD`).
* **Meta:** Identificar qué archivos funcionales se han modificado para determinar el alcance del cambio (Patch, Minor o Major) y qué se debe documentar.

#### 2. 🔄 Actualización de Variables de Sistema (Frontend)
* **Acción:** Localiza los archivos de configuración o constantes globales donde se define la versión de la aplicación o la fecha de build (ej: `package.json`, `version.ts`, `config.js`, `.env`, `settings.py`).
* **Requisito:** Incrementa la versión en estos archivos.
* **Importante:** Asegúrate de modificar la variable específica que el Frontend consume para mostrar la versión en la interfaz de usuario (UI), garantizando que el usuario final vea el cambio.

#### 3. 📝 Actualización de Documentación
* **Acción:** Basado en el "Análisis Diferencial" del paso 1, actualiza el archivo `CHANGELOG.md`.
* **Formato:** Agrega una nueva entrada con la versión definida en el paso 2, listando las mejoras o correcciones detectadas.

#### 4. 🚀 Generación de Comandos Git
* **Salida:** Genera un bloque de código bash con:
    1.  `git add .` (para incluir los cambios de código + los cambios de versión que acabas de hacer).
    2.  `git commit -m "..."` con un mensaje semántico (Conventional Commits) en español que refleje la naturaleza de los cambios detectados (ej: `chore(release): v1.2.0 - actualización de UI y lógica`).

**Nota:** Si detectas errores de sintaxis obvios en los archivos modificados, corrígelos antes de sugerir el commit.
```

-----

### 2\. Versión en Inglés (Optimizada para la IA)

Esta versión elimina la "decoración" innecesaria y utiliza terminología técnica directa (`git diff`, `variable scope`, `UI reflection`). Es más eficiente para modelos como GPT-4, Claude 3.5 Sonnet o Copilot, ya que reduce la ambigüedad.

**Copia y pega esto en tu herramienta de IA:**

```markdown
# Role: Senior DevOps & Release Engineer
# Task: Prepare codebase for a stable release by analyzing diffs, bumping versions, and generating git commands.

## Instructions:

1. **Analyze Context & Diffs:**
   - Compare the current working directory changes against the last commit (HEAD).
   - Identify the scope of changes to determine the semantic version bump (Patch/Minor/Major).

2. **Frontend Version Sync (Priority):**
   - Locate system configuration files (generic/agnostic search: e.g., `package.json`, `environment.ts`, `constants.js`, `.env`).
   - Increment the version number/build date variables.
   - **CRITICAL:** Ensure the specific variable updated is the one utilized by the Frontend to display the version string in the User Interface.

3. **Documentation:**
   - Update `CHANGELOG.md` reflecting the changes found in Step 1 and the new version number from Step 2. Maintain existing formatting.

4. **Git Execution:**
   - Verify code syntax integrity; fix minor issues if found.
   - Generate a code block with the following commands:
     - `git add .`
     - `git commit` using Conventional Commits format (e.g., `feat(ui): ...` or `chore(release): ...`). The commit message must be in SPANISH.

## Output Requirement:
Provide the modified code blocks for the version files, the changelog text, and the final git commands.
```

-----

### Cambios Clave Realizados:

1.  **Comparación con el último commit:**

      * *Español:* Se añadió el paso "Análisis Diferencial".
      * *Inglés:* Se especificó "Compare ... against the last commit (HEAD)". Esto le dice a la IA que no solo mire el archivo, sino *qué cambió* para decidir qué escribir en el commit.

2.  **Reflejo en el Frontend:**

      * Se añadió la instrucción explícita de buscar la variable que **consume la UI**. Muchas veces las IAs cambian el `package.json` pero olvidan el archivo `environment.ts` o `config.js` que es el que realmente lee la webapp.

3.  **Generalización:**

      * Se eliminaron referencias específicas (como Docker o scripts shell específicos) para dejarlo abierto a cualquier stack tecnológico, pero exigiendo que busque archivos de "configuración".

### ¿Te gustaría que haga el siguiente paso?

Puedo generar un **ejemplo de respuesta** simulando cómo contestaría la IA a este prompt en inglés, para que veas si el resultado es lo que esperas.